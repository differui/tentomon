$$Box-UI($ui-type, $brand-color, $brand-color-end, $border-color, $background-color, $background-disabled-color)
  .box-{$ui-type}
  .box-{$ui-type}-static
    @extend $BOX-UI

    if $ui-type is 'basis'
      color $black

    border-color $border-color
    background-color $background-color

    $$linear-gradient($brand-color, $brand-color-end)
    $$text-highlight()

    &:active
    &.is-active
      @extend $BOX-UI-ACTIVE

    &:disabled
    &.is-disabled
      @extend $BOX-UI-DISABLED

      background-image none
      background-color $background-disabled-color
    
      if $ui-type is 'basis'
        color $grey-light