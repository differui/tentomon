// Progress
//
// Size:
//
// + `.progress--xs`
// + `.progress--s`
// + `.progress--m`
// + `.progress--l`
// + `.progress--xl`
//
// Theme:
//
// + `.progress--clean`
// + `.progress--banding`
//
// State:
//
// + `half`
// + `done`
// + `waiting`
//
// Markup:
// <div class="progress progress--m" data-state="half waiting">
//   <span class="progress-tip">
//     <span class="progress-text progress-text--face">50%</span>
//   </span>
//   <span class="progress-text progress-text--back">50%</span>
// </div>
//
// Styleguide 2.8
.progress
  @extend .ui-basis

  width 100%
  position relative
  border-radius $b-rad-round

  padding-top $b-vgap + $b-bd-width
  padding-bottom @padding-top

// -------------------------------------
//   Modifiers
// -------------------------------------

// ----- Size ----- //

.progress--xs
  font-size $b-fs-xs

  .progress-text--face
    height $b-fs-xs

.progress--s
  font-size $b-fs-s

  .progress-text--face
    height $b-fs-s

.progress--m
  font-size $b-fs-m

  .progress-text--face
    height $b-fs-m

.progress--l
  font-size $b-fs-l

  .progress-text--face
    height $b-fs-l

.progress--xl
  font-size $b-fs-xl

  .progress-text--face
    height $b-fs-xl

// ----- Theme ----- //
.progress--clean
  .progress-text
    visibility hidden

.progress--banding
  border-radius $b-rad-square

  .progress-tip
    border-radius $b-rad-square

  .progress-text
    display none

// -------------------------------------
//   States
// -------------------------------------

.progress[data-state~='half']
  .progress-tip
    width 50%

.progress[data-state~='done']
  .progress-tip
    width 100%

.progress[data-state~='waiting']
  @extend .stripe-diagonal

  background-color $c-bd-dark

// -------------------------------------
//   Scaffolding
// -------------------------------------

.progress-tip
  @extend .center-left
  @extend .ui-safe

  z-index $b-idx-top
  overflow hidden
  border-radius $b-rad-round - 1

.progress-text
  @extend .ellipsis

  display block

  text-align center

.progress-text--back
  width 100%
  z-index $b-idx-bottom
  position relative

.progress-text--face
  @extend .center

  width 100%

  color $c-white
  line-height 1
